{% extends 'base.html.twig' %}

{% block title %}Registration{% endblock %}

{% block body %}
    <h1>Registration</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ registration.id }}</td>
            </tr>
            <tr>
                <th>Event</th>
                <td>{{ registration.event }}</td>
            </tr>
            <tr>
                <th>User</th>
                <td>{{ registration.user }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ registration.location }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('registration_index') }}">back to list</a>

    <a class="btn btn-secondary" href="{{ path('registration_edit', {'id': registration.id}) }}">edit</a>

    {{ include('registration/_delete_form.html.twig') }}
{% endblock %}
